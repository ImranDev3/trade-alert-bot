# Core bot framework (async, long-polling) with the JobQueue extra,
# needed for the background alert-polling job.
# 22.x is required for Python 3.13/3.14 (21.x uses a removed asyncio API).
python-telegram-bot[job-queue]==22.8

# HTTP client for market data APIs
requests==2.32.3

# WebSocket client for realtime Binance crypto prices
websockets==16.0

# .env configuration
python-dotenv==1.0.1
